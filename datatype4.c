#include<stdio.h>
int main ()
{
    int roll;
    float per;
    printf("Enter the roll and per =");
    scanf("%d%f",&roll,&per);
    printf("\n roll no. is =%d \n percentage is =%f",roll,per);
    return 0;
}