#include <stdio.h>
int main()
{
    int num=23;

    //conditionsto check if the negative/positive or zero
    if (num > 0)
            printf("the number is positive");
    else if (num < 0)
            printf("the number is negative");
    else
            printf("zero");
    return 0;
    
}