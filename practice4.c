#include<stdio.h>
int main()
{
    int a;
    printf(" Enter the no.=");
    scanf("%d",&a)
    if (a > 0)
    {
        printf(" is a positive= ");
    }
    else if( a < 0)
         {
           printf("is a negetive=");
         }
        else
        {
        printf("zero ");
        }
        printf

}